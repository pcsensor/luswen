# 贡献指南

## 开发流程

1. 从最新 `main` 创建短生命周期分支。
2. 修改时遵守 [架构约束](docs/architecture.md)：分层依赖、单一数据源、脚本最小化。
3. 新文章遵守 [内容模型](docs/content-model.md)。
4. 提交前运行下方「提交前检查」。
5. 对页面变化进行桌面端与移动端目视检查。

## 修改原则

- 优先修改现有抽象，不为一次性需求创建通用层。
- 站点名称、作者、导航、评论参数等全局信息只在 `src/config/site.ts` 修改。
- 纯逻辑（排序、过滤、分页、日期、阅读时长、搜索索引）放在 `src/lib/` 的纯函数模块并配套单元测试；访问 Content Collection 的查询封装在 `src/lib/posts.ts`。
- 页面仅负责组合组件，不在页面中复制业务规则（排序、分页、URL 拼装一律走 `lib`）。
- 新的客户端脚本必须有明确用途，限制在对应组件内部，并考虑无脚本、键盘操作和减少动态效果的用户。
- 新增第三方依赖前先说明理由；原生 HTML/JS 能满足的不加依赖。
- 不在源代码中保存密钥、令牌、私人地址或第三方凭据。

## 提交前检查

```bash
npm run check      # 架构边界 + 类型检查 + 单元测试
npm run build      # 完整构建
npm run preview    # 目视检查关键路由
```

关键路由：首页（含分页）、任一篇文章（含评论区状态）、归档、标签、搜索、404、`/rss.xml`、`/sitemap.xml`、`/robots.txt`。

改动 `src/lib/` 纯函数时，同步更新或新增对应的 `*.test.ts`。

如架构检查失败，应调整依赖方向；不要通过放宽规则来掩盖循环依赖。
