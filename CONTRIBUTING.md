# 贡献指南

## 开发流程

1. 从最新主分支创建短生命周期分支。
2. 修改时遵守 [架构约束](docs/architecture.md)。
3. 新文章遵守 [内容模型](docs/content-model.md)。
4. 提交前运行 `npm run check` 和 `npm run build`。
5. 对页面变化进行桌面端与移动端目视检查。

## 修改原则

- 优先修改现有抽象，不为一次性需求创建通用层。
- 站点名称、作者、导航等全局信息只在 `src/config/site.ts` 修改。
- 内容查询、排序、日期格式等逻辑集中在 `src/lib/`。
- 页面仅负责组合组件，不在页面中复制业务规则。
- 新的客户端脚本必须有明确用途，并考虑无脚本、键盘操作和减少动态效果的用户。
- 不在源代码中保存密钥、令牌、私人地址或第三方凭据。

## 提交前检查

```bash
npm run check
npm run build
```

如架构检查失败，应调整依赖方向；不要通过放宽规则来掩盖循环依赖。
